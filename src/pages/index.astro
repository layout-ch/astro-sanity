---
export const prerender = false;

const preferredLocale = Astro.preferredLocale;

if (preferredLocale) {
    return Astro.redirect(`/${preferredLocale}/`, 301);
} else {
    return Astro.redirect(`/en/`, 301);
}
---
